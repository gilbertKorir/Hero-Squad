{{#partial "content"}}
    <h1>My Heroes</h1><br>
    <div class="row">
        {{#if heroes}}
            {{#each heroes}}
                <a href="/heroes/{{id}}"><button class="btn btn-success">hero-info</button></a>
            {{/each}}
        {{/if}}
    </div>
{{/partial}}